apiVersion: console.openshift.io/v1
kind: ConsolePlugin
metadata:
  name: vm-activity-plugin
spec:
  displayName: "VM Activity History"
  backend:
    type: Service
    service:
      name: vm-activity-plugin
      namespace: vm-activity-plugin
      port: 9443
      basePath: "/"
  proxy:
    - alias: vm-activity
      authorization: UserToken
      endpoint:
        type: Service
        service:
          name: vm-activity-api
          namespace: vm-activity-plugin
          port: 8443
---
apiVersion: v1
kind: Service
metadata:
  name: vm-activity-plugin
  namespace: vm-activity-plugin
  labels:
    app: vm-activity-plugin
    app.kubernetes.io/name: vm-activity-plugin
    app.kubernetes.io/component: console-plugin
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: vm-activity-plugin-cert
spec:
  ports:
    - name: https
      protocol: TCP
      port: 9443
      targetPort: 9443
  selector:
    app: vm-activity-plugin
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vm-activity-plugin
  namespace: vm-activity-plugin
  labels:
    app: vm-activity-plugin
    app.kubernetes.io/name: vm-activity-plugin
    app.kubernetes.io/component: console-plugin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vm-activity-plugin
  template:
    metadata:
      labels:
        app: vm-activity-plugin
    spec:
      containers:
        - name: vm-activity-plugin
          image: quay.io/andy_krohg/vm-activity-plugin:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9443
              protocol: TCP
          volumeMounts:
            - name: nginx-conf
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
            - name: nginx-cert
              mountPath: /etc/nginx/certs
              readOnly: true
          resources:
            requests:
              cpu: 10m
              memory: 50Mi
            limits:
              cpu: 100m
              memory: 100Mi
      volumes:
        - name: nginx-conf
          configMap:
            name: vm-activity-plugin-nginx
        - name: nginx-cert
          secret:
            secretName: vm-activity-plugin-cert
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: vm-activity-plugin-nginx
  namespace: vm-activity-plugin
data:
  nginx.conf: |
    worker_processes auto;
    error_log /dev/stderr info;
    pid /tmp/nginx.pid;

    events {
      worker_connections 1024;
    }

    http {
      include /etc/nginx/mime.types;
      default_type application/octet-stream;

      access_log /dev/stdout;

      server {
        listen 9443 ssl;
        ssl_certificate /etc/nginx/certs/tls.crt;
        ssl_certificate_key /etc/nginx/certs/tls.key;

        root /usr/share/nginx/html;

        location / {
          try_files $uri $uri/ /index.html;
        }
      }
    }
